// /Namaste
#include<iostream>
#include<string>
using namespace std;
int main(){
int *ptr;
int a;
ptr=&a;
printf("Address of variable a in hexadecimal is: %p\n",ptr );
printf("Address of variable a in decimal is: %d",ptr );
return 0;
}
